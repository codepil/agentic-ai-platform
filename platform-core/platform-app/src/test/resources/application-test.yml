spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          # Use jwk-set-uri instead of issuer-uri in tests — avoids OIDC discovery HTTP call
          jwk-set-uri: https://test.okta.example.com/oauth2/default/v1/keys
  data:
    mongodb:
      uri: mongodb://localhost:27017/agent_platform_test

agent:
  engine:
    base-url: http://localhost:8000

slack:
  webhook-url: ""
  oncall-channel: "#test"
